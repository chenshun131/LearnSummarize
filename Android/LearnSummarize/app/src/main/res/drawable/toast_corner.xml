<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 主体填充色 -->
    <solid android:color="@color/themecolor4"/>

    <!-- 描边，边框宽度、颜色 -->
    <stroke
        android:width="1dip"
        android:color="@color/themecolor1"/>

    <!-- 边角圆弧的半径 -->
    <corners android:radius="20dp"/>

</shape>
